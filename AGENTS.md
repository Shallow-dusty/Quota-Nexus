# AGENTS.md — AI Quota Monitor

本文件约束在本仓库工作的 Agent。

## 读取顺序

1. `README.md`：项目目标、当前选择和下一步。
2. `docs/DESIGN.md`：实现基线、阶段计划和验收标准。
3. 只有需要追溯早期讨论时，才读取 `docs/archive/`。

## 工作方式

- `docs/DESIGN.md` 是当前设计基线，但不是不可修改的合同。
- 实现证据或用户需求改变时，直接同步更新设计；重大取舍在设计中简要写清理由即可。
- 不再强制每次修改都走双 Agent 提案循环。
- “首期不做”表示开发优先级，不代表永久禁止；不要用当前 MVP 限制后续产品演进。
- 保留已有历史，不把已归档评审重新当作现行约束。

## 实现约束

- 以 `DESIGN.md` 当前基线为准实现，不提前生成无用空壳。
- 用户凭据、未脱敏响应和本机私有数据不得提交进 Git。
- 修改外部客户端、发送模型请求或开启代理等有实际副作用的功能，开发和测试时必须显式启用。
- 实现发现设计假设不成立时，同步修正文档，不在代码与文档之间留下两套事实。
