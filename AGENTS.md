# AGENTS.md — AI Quota Monitor

本文件约束所有在本仓库工作的 AI agent（pi、Codex 等）。

## 读取顺序

1. `README.md`：确认产品边界和文档地图。
2. `STATUS.md`：确认当前阶段、阻塞和下一动作。
3. `docs/DESIGN.md`：读取现行设计、测试计划和验收门。
4. 只有需要追溯设计理由或提出变更时，才进入 `docs/proposals/`。

## 设计文档治理（设计阶段现行）

- `docs/DESIGN.md` 是设计的**单一事实来源**（SSOT），所有设计结论以它为准。
- 任何 agent **不得绕过提案直接修改** `DESIGN.md`。修改建议先写入
  `docs/proposals/PROPOSAL-NNN.md`（NNN 递增，不跳号）；只有审计方明确接受的
  条目，才可在同一轮由审计方按审计结论合并进 `DESIGN.md`。
- 治理循环：
  1. 一方提交提案；
  2. 另一方独立审计，逐条给出 accept / reject（reject 必须附理由）；
  3. 双方同意的条目合并进 `DESIGN.md`，同步更新版本号与"最后更新"日期；
  4. 争议条目在下一轮提案中补充论证、修改或明确放弃；
  5. 循环往复，直到双方无争议。
- 提案条目固定格式：**编号 / 目标章节 / 现状与问题 / 提议修改 / 理由 / 状态**。
  - 理由必须自洽、可验证——提案会经受对立审计，理由不足的条目会被驳回。
  - 状态流转：`pending → accepted | rejected | superseded`。
  - 审计意见记录在提案文件的 Audit 区（条目级引用编号），不改动原文。
- 条目间若有依赖（如某表字段被另一条目的策略使用），必须在条目中**显式声明依赖**。
- 已合并或已驳回的提案文档**保留不删**，作为决策记录（ADR 性质）。
- `docs/proposals/README.md` 只维护提案索引，不复述完整设计。

## 实现阶段

- 代码结构与目录约定以 `DESIGN.md` §19 为准。
- 提交前必须通过 `DESIGN.md` §15 安全检查清单中的适用项。
- 当前只允许从 `STATUS.md` 指定的 Phase 0 开始；不要提前生成整套应用空壳。
- 探针原始响应必须先脱敏再形成 fixture 或 provider contract，秘密不得进入 Git。
- 实现中发现设计假设不成立时，先记录证据并走提案流程，不在代码中静默改变合同。
